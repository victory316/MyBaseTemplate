package com.example.mybasetemplate.enums

/**
 * 인트로 페이지에서 보여주는 Feature를 구분하는 enum
 *
 * - 추가 가능성이 많은 enum 으로 확장성을 고려한 설계가 진행되어어야 함.
 */
enum class IntroFeatures {
    FEATURE_MATERIAL,
    FEATURE_WEATHER_API,
    FEATURE_COMPOSE
}